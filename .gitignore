# --- Data is NEVER redistributed -------------------------------------------
# Databento (GLBX.MDP3) and Yahoo Finance licenses forbid redistributing the
# raw series. Every consumer downloads it themselves with their own account:
# see the "Reproducing this" section of the README.
data/
*.csv
*.csv.gz
*.dbn
*.dbn.zst

# --- Secrets ---------------------------------------------------------------
.env
.env.*

# --- Local environment -----------------------------------------------------
venv/
.venv/
__pycache__/
*.pyc
.pytest_cache/

# --- Salida de la suite (se regenera en cada corrida) ----------------------
tests/fase2/ultima_corrida.log
